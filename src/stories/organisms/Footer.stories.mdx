import { ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import Footer from '../../components/organisms/Footer';

<Meta title="Organisms/Footer" component={Footer} />

# Footer

<Canvas>
    <Story name='Basic'>
        <Footer 
            r1c1={<div class='box' />}
            r1c2={<div class='box' />}
            r1c3={<div class='box' />}
            r1c4={<div class='box' />}
            r1c5={<div class='box' />}
            r1c6={<div class='box' />}
            r2c1={<div class='box' />}
            r2c2={<div class='box' />}
            r2c3={<div class='box' />}
            r2c4={<div class='box' />}
            r2c5={<div class='box' />}
            r3={<div class='box' />}
        />
    </Story>
</Canvas>