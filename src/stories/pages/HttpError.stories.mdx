import { ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import HttpError from '../../components/templates/HttpError';

<Meta title="pages/HttpError" component={HttpError} />

# HttpError Page

<ArgsTable of={HttpError} />

<Canvas>
    <Story name='404'>
        <HttpError 
            status='Oops! Page not found'        
            status_code = '404'
            description = 'we are sorry, but the page you requested was not found'
         />
    </Story>
</Canvas>