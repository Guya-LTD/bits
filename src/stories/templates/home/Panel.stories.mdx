import { ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import PanelTemplate from '../../../components/templates/home/Panel';
import Sidebar from '../../../components/molecules/Sidebar';

<Meta title="Templates/Home/Panel" component={PanelTemplate} />

# Login Templates

<Canvas>
    <Story name='Devault'>
        <PanelTemplate 
            sidebar={ 
                <Sidebar 
                    menu={
                        {
                            primary: {
                                logo: <div class='box' />,
                                top: <div class='box' />,
                                bottom: <div class='box' />
                            },
                            secondary: <div class='box' />
                        }
                    }
                />
             }
            header={ <div class='box' /> }
            footer={ <div class='box' /> } >
            <div class='box' />
        </PanelTemplate>
    </Story>
</Canvas> 