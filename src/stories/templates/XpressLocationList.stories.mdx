import { ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import XpressLocationList from '../../components/templates/XpressLocationList';
import Footer from '../../components/organisms/Footer';

<Meta title="Templates/XpressLocationList" component={XpressLocationList} />


# XpressLocationList

<Canvas>
    <Story name="Basic">
        <XpressLocationList
            header={<div class="box" />}
            search={<div class="box" />}
            location={{
                left: [
                    <div class='box' />,
                    <div class='box' />
                ],
                right: <div class='box' />
            }}
            footer={
                <Footer
                    r1c2={<div class='box' />}
                    r2c3={<div class='box' />}
                    r2c4={<div class='box' />}
                    r3={<div class='box' />}
                />
            }
        />
    </Story>
</Canvas>